<?php


// Operador 'E' AND &&

$estudando = true;
$empregado = false;

var_dump($estudando && $empregado);

// Operador 'OU' OR ||

var_dump($estudando || $empregado);

// Operador 'Ou Exclusivo' XOR ^

var_dump($estudando ^ $empregado);

// Operador 'NÃO' NOT !

var_dump(!$estudando);